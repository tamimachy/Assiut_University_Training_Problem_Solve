#include<bits/stdc++.h>
#include<algorithm>
#include <vector>
using namespace std;
int main(){
    int t, a,b,i;
    cin >> t;
    vector<long long> n(t);
    for(i=0;i<t;i++){
        cin >> n[i];
    }
    for(i=0;i<t;i++){
        if(n[i]%12!=0 && n[i]<10){
            cout << -1;
        }else if()
    }
        cout << n[i] << " " ;
    return 0;
}
